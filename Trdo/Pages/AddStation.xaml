<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Trdo.Pages.AddStation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*,Auto" RowSpacing="12">

        <!--  Title  -->
        <TextBlock
            x:Uid="AddStation_PageTitle"
            Grid.Row="0"
            FontSize="20"
            FontWeight="SemiBold" />

        <!--  Content Area  -->
        <ScrollView Grid.Row="1">
            <StackPanel Spacing="12">
                <!--  Station Name Input  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddStation_StationNameLabel" FontWeight="SemiBold" />
                    <TextBox
                        x:Name="StationNameTextBox"
                        x:Uid="AddStation_StationNamePlaceholder"
                        Text="{x:Bind ViewModel.StationName,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <!--  Stream URL Input  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddStation_StreamUrlLabel" FontWeight="SemiBold" />
                    <TextBox
                        x:Name="StreamUrlTextBox"
                        x:Uid="AddStation_StreamUrlPlaceholder"
                        Text="{x:Bind ViewModel.StreamUrl,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!--  Homepage URL Input  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddStation_HomepageLabel" FontWeight="SemiBold" />
                    <TextBox
                        x:Name="HomepageTextBox"
                        x:Uid="AddStation_HomepagePlaceholder"
                        Text="{x:Bind ViewModel.Homepage,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!--  Favicon URL Input  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddStation_LogoUrlLabel" FontWeight="SemiBold" />
                    <TextBox
                        x:Name="FaviconUrlTextBox"
                        x:Uid="AddStation_LogoUrlPlaceholder"
                        Text="{x:Bind ViewModel.FaviconUrl,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </StackPanel>
        </ScrollView>

        <!--  Buttons  -->
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Click="CancelButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE711;" />
                    <TextBlock x:Uid="Action_Cancel" />
                </StackPanel>
            </Button>

            <Button
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                Click="SaveButton_Click"
                IsEnabled="{x:Bind ViewModel.CanSave,
                                   Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE74E;" />
                    <TextBlock x:Uid="Action_Save" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
