<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Trdo.Pages.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Trdo.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Trdo.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:CountToEmptyStateVisibilityConverter x:Key="CountToEmptyStateVisibilityConverter" />
        <converters:CountToHasItemsVisibilityConverter x:Key="CountToHasItemsVisibilityConverter" />
    </Page.Resources>

    <Grid RowDefinitions="Auto,*" RowSpacing="8">
        <!--  Page Title  -->
        <TextBlock
            Grid.Row="0"
            FontSize="16"
            FontWeight="SemiBold"
            Text="Favorites" />

        <!--  Content  -->
        <Grid Grid.Row="1" Padding="0,0,0,0">

            <!--  Empty State  -->
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Visibility="{x:Bind ViewModel.Favorites.Count, Mode=OneWay, Converter={StaticResource CountToEmptyStateVisibilityConverter}}">
                <FontIcon
                    HorizontalAlignment="Center"
                    FontSize="48"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Glyph="&#xE735;" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="16"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="No favorites yet"
                    TextAlignment="Center" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    Text="Tap the star icon on tracks you love to save them here"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>

            <!--  Favorites List  -->
            <ListView
                Padding="0,0,12,0"
                x:Name="FavoritesListView" 
                ItemsSource="{x:Bind ViewModel.Favorites, Mode=OneWay}"
                SelectionChanged="FavoritesListView_SelectionChanged"
                SelectionMode="Single"
                Visibility="{x:Bind ViewModel.Favorites.Count, Mode=OneWay, Converter={StaticResource CountToHasItemsVisibilityConverter}}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0,4" />
                        <Setter Property="Margin" Value="0,0,0,4" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:FavoriteTrack">
                        <Grid
                            Padding="12"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  Main Row: Track Info and Remove Button  -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!--  Track Info  -->
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <!--  Title/Display Text - wraps when expanded  -->
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Text="{x:Bind DisplayText}"
                                        TextWrapping="Wrap" />

                                    <!--  Station Name  -->
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <FontIcon
                                            VerticalAlignment="Center"
                                            FontSize="10"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            Glyph="&#xE95A;" />
                                        <TextBlock
                                            FontSize="11"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            Text="{x:Bind StationName}"
                                            TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </StackPanel>

                                <!--  Remove Button  -->
                                <Button
                                    Grid.Column="1"
                                    Padding="8"
                                    VerticalAlignment="Top"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Click="RemoveFavorite_Click"
                                    Tag="{x:Bind}"
                                    ToolTipService.ToolTip="Remove from favorites">
                                    <FontIcon
                                        FontSize="16"
                                        Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                        Glyph="&#xE735;" />
                                </Button>
                            </Grid>

                            <!--  Expanded Row: Search Links (shown when selected)  -->
                            <StackPanel
                                x:Name="ExpandedContent"
                                Grid.Row="1"
                                Margin="0,8,0,0"
                                Orientation="Horizontal"
                                Spacing="16"
                                Visibility="Collapsed">

                                <!--  Search on Spotify  -->
                                <HyperlinkButton
                                    Click="SpotifyLink_Click"
                                    Padding="4"
                                    Tag="{x:Bind}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <Image Width="16" Height="16">
                                            <Image.Source>
                                                <SvgImageSource UriSource="ms-appx:///Assets/spotify.svg" />
                                            </Image.Source>
                                        </Image>
                                        <TextBlock FontSize="11" Text="Spotify" />
                                    </StackPanel>
                                </HyperlinkButton>

                                <!--  Search on Discogs  -->
                                <HyperlinkButton
                                    Click="DiscogsLink_Click"
                                    Padding="4"
                                    Tag="{x:Bind}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon FontSize="12" Glyph="&#xE721;" />
                                        <TextBlock FontSize="11" Text="Discogs" />
                                    </StackPanel>
                                </HyperlinkButton>

                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
