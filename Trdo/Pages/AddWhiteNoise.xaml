<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Trdo.Pages.AddWhiteNoise"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*,Auto" RowSpacing="12">

        <!--  Title  -->
        <TextBlock
            x:Uid="AddWhiteNoise_PageTitle"
            Grid.Row="0"
            FontSize="20"
            FontWeight="SemiBold" />

        <!--  Content Area  -->
        <ScrollView Grid.Row="1">
            <StackPanel Spacing="12">
                <!--  Station Name Input  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddWhiteNoise_StationNameLabel" FontWeight="SemiBold" />
                    <TextBox
                        x:Name="StationNameTextBox"
                        x:Uid="AddWhiteNoise_StationNamePlaceholder"
                        Text="{x:Bind ViewModel.StationName,
                                      Mode=TwoWay,
                                      UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <!--  Noise color  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddWhiteNoise_ColorLabel" FontWeight="SemiBold" />
                    <ComboBox
                        HorizontalAlignment="Stretch"
                        SelectedIndex="{x:Bind ViewModel.NoiseColorIndex, Mode=TwoWay}">
                        <ComboBoxItem x:Uid="AddWhiteNoise_White" />
                        <ComboBoxItem x:Uid="AddWhiteNoise_Pink" />
                        <ComboBoxItem x:Uid="AddWhiteNoise_Brown" />
                        <ComboBoxItem x:Uid="AddWhiteNoise_Blue" />
                        <ComboBoxItem x:Uid="AddWhiteNoise_Violet" />
                        <ComboBoxItem x:Uid="AddWhiteNoise_Grey" />
                    </ComboBox>
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind ViewModel.ColorDescription, Mode=OneWay}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <!--  Volume  -->
                <StackPanel Spacing="4">
                    <TextBlock x:Uid="AddWhiteNoise_VolumeLabel" FontWeight="SemiBold" />
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <Slider
                            Grid.Column="0"
                            AutomationProperties.Name="Volume"
                            Maximum="200"
                            Minimum="0"
                            StepFrequency="1"
                            Value="{x:Bind ViewModel.VolumePercent, Mode=TwoWay}" />
                        <TextBlock
                            Grid.Column="1"
                            MinWidth="48"
                            VerticalAlignment="Center"
                            Text="{x:Bind ViewModel.VolumeDescription, Mode=OneWay}" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </ScrollView>

        <!--  Buttons  -->
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Click="CancelButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE711;" />
                    <TextBlock x:Uid="Action_Cancel" />
                </StackPanel>
            </Button>

            <Button
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                Click="SaveButton_Click"
                IsEnabled="{x:Bind ViewModel.CanSave,
                                   Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE74E;" />
                    <TextBlock x:Uid="Action_Save" />
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Page>
